<h1>Lists#show</h1>

<%= @list.name %>

<%= link_to "Add a bookmark", new_list_bookmark_path(@list_id)  %>
<p>
  <%= link_to "Delete the list", list_path(@list), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" } %>
</p>

<p>
  <%= link_to "Back to Lists", lists_path  %>
</p>
